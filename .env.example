# =================================================
# ARQUIVO DE EXEMPLO DE VARIÁVEIS DE AMBIENTE
# Copie este arquivo para .env e preencha com seus valores.
# NÃO adicione o arquivo .env ao Git.
# =================================================

# --- Configurações do Banco de Dados PostgreSQL ---
# Senha para o usuário do banco de dados. DEVE ser alterada.
POSTGRES_PASSWORD=minha-senha-super-segura-para-o-banco
POSTGRES_USER=keycloak
POSTGRES_DB=keycloak

# --- Configurações do Keycloak ---
# Credenciais do usuário administrador inicial do Keycloak.
# A senha DEVE ser alterada para algo seguro.
KEYCLOAK_ADMIN=admin-temp
KEYCLOAK_ADMIN_PASSWORD=minha-senha-super-segura-para-o-admin

# Hostname que o Keycloak usará para gerar os links.
# Para desenvolvimento local, 'localhost' é suficiente.
# Para produção, deve ser o seu domínio público (ex: sso.meudominio.com)
KC_HOSTNAME=localhost
